## 1. Защита секретов

- [x] 1.1 Создать `.gitignore` в корне репозитория (`.env`, `run_mcp.sh`, `MCP_Distr/`, бэкапы, данные БД)
- [x] 1.2 Проверить `mcp-deployment/.env.example` — убедиться что нет реальных ключей/паролей (заменить если есть)
- [x] 1.3 Проверить docker-compose файлы в `FormsServer/`, `Graph_metadata_search/` — убрать hardcoded-секреты, заменить на переменные
- [x] 1.4 Проверить `mcp-deployment/docker-compose.graph.yml` — убедиться что default-значение Neo4j-пароля безопасно для публикации

## 2. Документация

- [x] 2.1 Создать `README.md` — описание проекта, список серверов, quick start, ссылки на документацию
- [x] 2.2 Актуализировать `CLAUDE.md` — убрать/обновить что нужно для публичного репозитория

## 3. Инициализация git и публикация

- [x] 3.1 Выполнить `git init`, добавить файлы, проверить `git status` — убедиться что ни один секрет не попадает в staging
- [x] 3.2 Выполнить `git diff --cached` — финальная проверка на секреты в staged файлах
- [x] 3.3 Создать первый коммит
- [x] 3.4 Создать репозиторий на GitHub и выполнить push
